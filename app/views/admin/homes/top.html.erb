<h4>投稿一覧</h4>
<table class="table table-hover table-invrse">
  <thead>
   <tr>
    <th>投稿id</th>
    <th>お酒の名前</th>
    <th>投稿日時</th>
    <th>投稿者の名前</th>
   </tr>
  </thead>
  
  <tbody>
   <% @posts.each do |post| %>
   <tr>
    <td><%= post.id %></td>
    <td><%= link_to liquor.name, admin_liquor_path(liquor.id) %></td>
    <td><%= link_to created_at.strftime('%Y/%m/%d'), admin_post_path(posts.id) %></td>
    <td><%= link_to customer.name, admin_customer_path(customer.name) %></td>
   </tr>
      
    <% end %>
  </tbody>
</table>

 