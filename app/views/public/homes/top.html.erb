<div class="container">
 <h2>ようこそ
  このサイトはお酒のレビューをするサイトです。</h2>
<h4>新しい投稿</h4>
<table class="table table-hover table-invrse">
 <thead>
  <tr>
   <th>画像</th>
   <th>お酒の名前</th>
  </tr>
 </thead>
 <tbody>
 <% @posts.last(4).each do |post| %>
  <tr data-href="<%= root_path(post) %>">
  <td><%= image_tag post.get_image(100,100) %></td>
  <td><%= post.liquor_name %></td>
 </tr>
<% end %>
</tbody>
</table>
 <%= link_to "全ての投稿を見る", public_posts_path, class: "btn btn-outline-primary" %>
 
